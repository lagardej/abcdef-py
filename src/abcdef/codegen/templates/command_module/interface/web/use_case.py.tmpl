"""Web handler stub for $use_case_name."""


def $use_case_name() -> object:
    """Web handler: $use_case_name.

    Receives a form submission, dispatches $use_case_pascal command via the bus,
    and returns an HTML response.

    TODO: Accept request object, parse form fields, build and dispatch command.
    TODO: Return redirect or rendered template on success/failure.
    """
    raise NotImplementedError
